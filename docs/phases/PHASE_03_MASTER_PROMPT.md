# Phase 3 Master Prompt

## Objective
Implement transport-independent messaging, dynamic routers, middleware chains, RPC tracking managers, and local Event Buses.

## Scope
- Create `MessageMetadata` and `MessageContext` primitives.
- Code `MessageRouter` dynamic registries.
- Implement the core `MessageBus` to track RPC loops.
- Build local asynchronous event-driven `EventBus`.
