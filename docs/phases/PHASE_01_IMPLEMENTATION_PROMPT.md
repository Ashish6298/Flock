# Phase 1 Implementation Prompt

## Tasks
1. Write project metadata and options to `pyproject.toml` file.
2. Formulate target errors inside `src/flock/exceptions.py`.
3. Put type definitions and protocols under `src/flock/types.py` and `src/flock/interfaces/`.
4. Implement configuration schemas using Pydantic inside `src/flock/config.py`.
5. Verify setup via basic import unit test coverage.
