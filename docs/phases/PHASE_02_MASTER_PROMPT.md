# Phase 2 Master Prompt

## Objective
Implement networking transports, protocol serialization layers, and binary packet envelope structures.

## Scope
- Create `JsonSerializer` and `MsgpackSerializer` engines.
- Define binary header frames with validation metrics.
- Develop the asynchronous TCP transport server/client streams.
